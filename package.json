{
  "name": "homelab",
  "version": "0.1.0",
  "description": "Security-hardened homelab infrastructure with Nx distributed task execution",
  "private": true,
  "scripts": {
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all"
  },
  "engines": {
    "node": ">=22.17.0",
    "pnpm": ">=10.22.0"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "nx": "22.0.3",
    "@nx/js": "22.0.3",
    "@nx/node": "22.0.3",
    "@nx/workspace": "22.0.3",
    "typescript": "^5.7.0"
  }
}
