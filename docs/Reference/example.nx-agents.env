# Example Nx distributed agents configuration (encrypted with SOPS)
# Generate secret with: bash scripts/generate-nx-agent-secret.sh
# Encrypt with: just nx-encrypt-config

# Shared secret for agent authentication (256-bit hex)
# Migration path: See docs/Nx Agent Migration.md for per-agent keypairs
NX_SHARED_SECRET=REPLACE_WITH_OUTPUT_FROM_generate-nx-agent-secret.sh

# Comma-separated list of Tailscale IPs for distributed agents
# Discover with: tailscale status | grep '100\.' | awk '{print $1}'
# Example: NX_AGENT_IPS=100.64.0.10,100.64.0.11,100.64.0.12
NX_AGENT_IPS=

# Optional: Nx Cloud cache server endpoint (defaults to local HTTP server)
# NX_CACHE_SERVER_URL=http://100.64.0.10:3333

# Optional: Maximum parallel agents (defaults to discovered agent count)
# NX_MAX_PARALLEL_AGENTS=3
