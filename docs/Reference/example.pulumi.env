# Example Pulumi environment variables
# Copy to infra/pulumi.env and encrypt with SOPS to create infra/pulumi.env.sops

# Pulumi access token (for Pulumi Cloud backend)
# Generate at: https://app.pulumi.com/account/tokens
PULUMI_ACCESS_TOKEN=pul-...

# Alternative: Use local file backend (no cloud token required)
# Uncomment to use local backend instead of Pulumi Cloud (preferred XDG-style):
# PULUMI_BACKEND_URL=file://$PULUMI_LOCAL_STATE  # where PULUMI_LOCAL_STATE defaults to ${XDG_STATE_HOME:-$HOME/.local/state}/pulumi

# AWS credentials (if deploying to AWS)
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_DEFAULT_REGION=us-east-1

# Azure credentials (if deploying to Azure)
ARM_CLIENT_ID=...
ARM_CLIENT_SECRET=...
ARM_SUBSCRIPTION_ID=...
ARM_TENANT_ID=...

# GCP credentials (if deploying to GCP)
GOOGLE_CREDENTIALS=...
GOOGLE_PROJECT=...
GOOGLE_REGION=us-central1
